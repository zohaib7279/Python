# step 1: start
# step 2: ask user for triangle hight input
n = input("Enter the triangle hight = ")
# step 3: create the loop in print the triangle shape with stars(*) based on user input
for i in range(1,int(n)+1):
    # step 4: print the triangle shape with stars(*)
    print(" "'*' * i)
# step 5: end